# Analysis: Using SWEBOK Requirements Engineering - Applied to Semantic System Design

> *Purpose:* This document analysis the identified needs of a problem requirements elicitation process to determine what are suitable requirements.

> *Slug*: Semantic System Design - Categorical Approach to SWEBOK Requirements Process

> *Location*: Development Journal/Notes/

> *Date Created:* 2025-03-01

> *Unique Identifier:* ANALYSIS-20250301-A-000

> *Document Schema:* oa-specification:documents/Requirement#AnalysisDocument

> *Related Resources:* REQUIREMENT-20250301-A-000 ELICITATION-20250301-A-000

> *Tags:* #SWEBOK #RequirementsEngineering #CategoryTheory #SemanticWeb #SystemDesign

## Introduction

The main reason this process has been started is due to the cognitive overload, and lack of visibility into the structure (in a way thats easy). So we will begin building the following processes.

Identifiying Requirements is the first step. The plan is:

    A) Define Requirements Documents
    B) Define Tasks Documents
    C) Semantically Link the documents
    D) Generate interface that works via a local PWA Web App, and via the termainal
    E) Ensure structurally infered semantics is consistent and easy to just think without worrying about the mistakes

This is a lot of work, so I will need some AI help. So I want to just write the ideas I have, and have the AI just fix it to be according to the specification.

    F) Generate a Simple terminal-ai agent, that is specifically ensures (E) is achieved, 

I have removed the need for Requirements to be processed as that should be autogenerated from the notes. Though I might need to do something at the end. Since I have to go back to the Requirements Document and make a tool that extracts all the requirements and formats them nicely as Structured Resource Specifications. So I support I will need to figure out tasks, after that. It will be an iterative cycle. But I have plans to use superceded linked semantic information.


## Discussion: Need to have well defined structure before using the system.

### Core Requirements Process

Let's begin by using ideas from the Resource Theory part of *Seven Sketches in Compositionality* by Spivak and Fong, and see if we can use it to break down the SWEBOK Requirements Processes.

**Problem → Requirement**

![Figure 1](assets-2025-03-01/figure-1.svg)

There are two distinct steps. So lets consider each block will have its own notes.

```TODO
Add reference to SWEBOK
```


#### Discussion: Need to make generated documents by identified without initial slugs

We need to start automating documents and using AI, such  that we can embed semantics via structures.

Because specialised AI agents that work on a task based on the semantics, documents will be genereated for humans to start work on. 

Each document needs to have a unique id as these will be autogenerated.

The preamble example allows us to generate a has based on meta data that is unique to the date/schema/ and need one more i might make two schemas on the same day. Maybe add a random value somewhere on generation. 

```
> Calculate the size of the collision domain.
    A-F{4}: 6 characters, 4 permutations
    6^4
    0-9{4}: 10 characters, 4 permutations
    10^4
    A-F{4} 0-9{4}: (6^4) * (10^4)
    In total ABCD1234EFGH7890 has space of: `(6^4*10^4)^2`
```

But its too unreadable.

So maybe use a shortcode. And since we do have some immutable facts, and a way of having multiple resources created on the same day. And the last 3 digits can be used to iterate with an AI. So 001-proposal suffix could be used. Though once the file has been indexed. it only needs up to the *-A bit, like in documents where multiple references have the same citation if not the suffixed letter. So when the number is replaced with the SLUG when the file has been indexed or transformed, whatever happens with the other versions, have a way of rolling back time.

> REQDOC20250301-A-000 or maybe make it so it can update versions with A000 and if collision when adding a new one use some Last Processed time.

>     GENERATED_ID ::= <SHORT_CODE><NUMERICAL_DATE>-<ALPHA CHAR>-<VERSION_IDENTIFIER>

Also the lexigraphical order of the SHORTCODE, will make it easy to process different types of documents in the same folder. Like Analysis Notes DOC-NOTES-20250301-A is actually before I started the analysis.

> **Requirement:** Create specify Requirements Documents, and the associated Requirements.Notes#Elicitation, Requirements. s specification using SWEBOK as a resource.

> **Requirement:** Specification Of Unique Resource Identifiers needed.

### Discussion: Need to ensure that the semantic ontology is generated.

> **Requirement:** AI agents that make improvements or fix up errors when it is clear that something should be tagged or referenced. Include notes from `DOC-NOTES-20250301-A` and the use of lexiographical. Operations should be simple and be applying rules to schema.


> **Requirement:** I need to be able to break down the analysis into smaller chunks.

### Discussion: Need to avoid migration hell

So this puts into consideration what if the schema changes. Well the system already has a way of dealing with adjust and syncing systems but this will need to be formalised.

But I'm actually thinking we need to model the user story.

1. Theres is a problem
2. So the first process is deciding what is required to solve said problem.
3. Process involves elucidating what is needed to solve a problem.
4. Then I need to analyse what I can do that solves those problems.

> **Requirement:** The elucidation notes should provide the problems and needs identified and what is needed to solve it. Keeping the requirements document tidy, but can be looked up. The requirements document should references the Elucidation Notes, and Analysis Notes, and vice versa. And the elucidation notes (make a child, sibling, parent hierarchy in the documents).

> **Requirement:** The requirement activity, should create the analysis once the first task is complete, any changes to the needs or requirements are resolved in the analysis.

> **Requirement:** Identified tasks in `DOC-NOTES-20250301-A to be done for an AI agent, should define the tone in the vocab semantics.

> **Requirement:** Requirement Documents and Notes are required to have the structure laid out correctly. `DOC-NOTES-20250301-A` should be superceded with the specification, and superceded should be added.

## Discussion: Need to have linked meta data.


>    **Todo:** Cluster everything together

>    **Todo:** Finish the resources materials and review the elucidation step.

>   **Todo:** Research and take note of any inaccuracies.

>    **Requirement:** Ensure assets used in notes are included in the specification.
    
>   **Note:** Perhaps I should structure it as question,answer or Need and Decision how could I naturally structure that.

>   **Note:** I should first formalise the URI semantics. 

>   **Todo:** Design a tool and put it in notes that tracks quick implementations so that I can easily extract the notes. Since improving my process could go a long way. Make it so I can diagram the screen.

#### Notes

Steps:

1. Create a requirements document `Development Journal/Requirements/<YYYY-MM-DD> <Slug>.md`  (a document resource «oa-document:requirement».

>   **Task:**
> Create cli tool which allows for `oa requirement elicit`, which creates a new document with the status of elicitation

2. List problems and use this to identifies objectives, goals and other things to address the problems, refer to the specification at «oa-specs:oa-document:requirement»

3. Use tools or specialised AI tool to make sure it matches the specification.

4. Now use the document status to analysis using `oa requirement analyze` which will create study notes so some research can be done.

5. When enough research is done, and requirements are listed in the document, and any other notes, ideas, and tasks that come up is finalised run the `oa requirement complete`

## Appendix: WIP Modelling

![Figure 2](assets-2025-03-01/figure-2.svg)


## Appendix: Identified Tasks

- Get the AI to get relevant sources to the schemas.

- Update Relevant GRAMMAR Syntax

- Ensure the Gramms

## Change Log

None.
# Requirements Study Notes

> *Purpose:* This document analysis performs some research when deciding how to fix the gaps of my knowledge.

> *Slug*: Semantic System Design - Categorical Approach to SWEBOK Requirements Process

> *Location*: Development Journal/Notes/

> *Date Created:* 2025-03-02

> *Unique Identifier:* DOC-NOTES-20250302-A-000

> *Document Schema:* oa-specs:schema/oa-document/Documents#Requirement.Notes/Elicitation

> *Related Resources:* DOC-REQ-20250301-A-000 DOC-NOTES-20250302-A-000

> *Tags:* #SWEBOK #RequirementsEngineering #CategoryTheory #SemanticWeb #SystemDesign

>   DOC-NOTES-20250302-A-000
Pressman and Maximum (Chatper 4 p.p54-74) defines requirements

The 'oa:spec:requirements#elicitation' should ensure that needs are derived from the goals and problems 
within a natural reading.

"Use breadth-first approach [...] to get the big picture of the project" Pressman and Maximum p.58.

And therefore in the requirements document, for each requirement, we need to have a requirement that has the following things, the needs are a product of the goals and problems faced (we call this the problem statement).

Need to use Contraints in the Analyse to create a feature list.

Pressman and Maxim (p.103 to p. 106) outlines requirements engineering
Defined to encompass.

>   1. Inception
>   2. Elicitation 
>   3. Elaboration
>   4. Negotionation
>   5. Specification
>   6. Validation

The whole process can be simplified as
---problems--|              ---goals---|
             |---needs----   ---needs--|----requirements----
---goals-----|              -problems--|
        Elicitation                 Analysis


The needs are derived from having a complain section, where the problems identified are numbered for internal reference.

The goals are discussed when in the Consideration section...

The things I need are added as formatted indent blocks, by myself, or an AI turning my freeform things so that the structure is according to specification so that everything is tracable due to each document having semantic metadata that point the the document that is used for...


... allows for a Requirement document, and a Elicitation Notes, and Analysis Notes, which are semantically maded in the document. 

Specification for the document will define the sections and questions and rules of the outputs and 
the structure of the document.

So the AI can format my code, or add inline needs or problems or goals that weren't picked up by me but the AI found enough context to put it in for me (which I review).

The success of the requirement will be measured by how useful it is for me to write the next requirements document to do the tasks.

Specifications will need to have headings to model this.

So om the spec I need to ensure that I cover the SWEBOK nomenclature.

>   Requirements Engineering
    Inception
    ... Covered by making a tool that creates these linked documents
    Inception
    ... Covered by writing the Elicit Notes
    Elaboration
    ...Covered by writing the Analysis Notes
    Negotiation
    ...Covered by finishing Restrospective Specs... due for completion by the end of week.
    Specification
    ...Covered with the completion of specifications of Requirements,Elicit Notes, Analysis Notes
    Verification
    ...will be verified by making an actual useful AI agent, such that the extraction tools are able to retrieve every entity in the graph above.

So finally the requirements are need to be given a prioriity, how often will i use this, how much will ihelp. Well this will help

Need to get all the questions and answers to train the system to learn at differnet layers. Using the different agents semantics to be able to distill the output in like a backwards propagation between distillation based on there varied experience.

The requirements are ...
... Use this and the other documents to define the task of the Requirements Specification...
... I really need to make the AI in the system other wise i will lose focus ...
... maybe make a task that greps the files and orders them by lexiographical order, and automate what was i doing, and it should be able to show you the last things written. So it would be like, that file was modified last. So build like a activity audit to realign myself, and possibly have an AI agent.

I will pass these three documents to the new chatgpt 4.5 model to see if it can help.

Lets finish by looking at making a list of requirements (checklists) to ensure each thing is correct, and for feedback on the checklist. Compliance rubric agent to provide feedback.

Need to be able to test the results. So I will need to take a diff, from different models, and see if they improve. In fact try it once without the specification file, and the one with the requirements specification. Since Each AI with have its own memory cache and learnt problems. 
Perhaps it could optimise its own memories by A/B testing. Train it by making mistakes on purpose and punshing it everytime it gets it wrong. -Train it to mess with me, to learn more about the VABES. Like in Minsky- 

p. 109 Traceability is achieved using these semantic models.

Well requirements are understood to be need if we ensure the goals and problems are correctly use to define needs.
and the the requirements addresses the needs. describe how it will be used or how it will remain a main stay. Which is strategic decision, to make something good like a specification.

The only issue is that i at least need to make the generator for the documents so that means the URI,URN, things must be done, as if there are no automatic embedding, and AI job to allow me to quickly type and it knows the conditions required to meet.


