# Collections

  - Synopsis

    - Collections fall into two categories: (1) 'OA Modules' which map specifications to configuration parameters such that each collection will allow for configuration of subsystems used to build a large system; and (2) 'OA Systems' which at the time of writing, is used for prepared system configurations that form the initial system architecture envisioned.

      - TLDR: Collections.OA_Modules = Roles like Storage and Network that can be used to reproducibly rebuild infrastructure from a yaml file.

      - TLDR: Collections.OA_Systems = Playbooks that build the system the OA System runs in, like a live persistent USB or PXE Booted RAM Hypervisor in OA System.

    - All Fully Qualified Collection Names are under `orchestration_architect.<COLLECTION>`

  - OA Infrastructure Catalog

    - OA000 Restore, Recover, Rebuild

      - Contains the OA001 Bootstrap

      - Contains the Information to Automate Recovery (Allows for Rebuilding, Migrating OA001)

      - 

    - OA001 Encrypted Master Control Node

      - Profile 1: Persistent LiveUSB

        - Build Environment: Artifacts Storage

        - Target: Any Block Device accessible via emulation or bare metal

      - Profile 2: Ephemeral Emulation

        - Build Environment: Loop device file in ephemeral RAM backed storage

        - Target: Any device accessible to the container runtime.

    - OA002 Ephemeral Transient Immutable RAM-backed Hypervisor

    - OA999 Demo System

  - OA Modules Catalogue

    - 'Runtime' Collection

      - runtime.qemu

      - runtime.nspawn

    - 'Storage' Collection

      - Roles

        - storage.storage

          - Parameters: btrfs_subvolume, mount_table, filesystem, loop_device, luks, partition_table

          - Usage: `ansible.builtin.include_role: name=orchestration_architect.storage.storage storage_mount_table= {...} storage_state='mounted'

          - States: ... list states ...

        - storage.btrfs_subvolume

        - storage.mount_table

        - storage.filesystem

        - storage.loop_device

        - storage.luks

        - storage.partition_table

      - Playbooks

        - RAM-backed Transient Volume

    - 'Meta' Collection 

      - Metadata

        - FQCN: orchestration-architect.meta

        - cURI: oa.collections:meta/

      - Roles

        - meta_template

        - meta_role

          - parameters

            - metadata

              - Adds the field 'metadata' as a supported configuration parameter to roles generated by orchestration-architect.meta.meta_role.

              - Metadata

                - In determining what metadata to store

                  - Consider

                    - cURI: OA.ROLES:META/meta_role

                    - FQCN: orchestration-architect.meta.meta_role

                  - An easier way to store this would be

                    - namespace: OA.Roles

                    - collection: META

                    - instance: meta_role

                  - As this would make making functors between file paths, specification files, naming conventions so much easier.

                - Note: this way i can simply the functor between a roles FQCN and cURI

                  - FQCN -> C_{n} -> cURI

                    - orchestration-architect -> C_{1} -> OA.ROLES

                    - .meta -> C_{2} -> /META 

                    - .meta_role -> C_{3} -> /meta_role

                  - FQCN and cURI is unique up to isomorphism (meaning its the same unless you change it to not be a 'role', because then FQCN doesn't exist, but if you change back you still get the same results no matter what way you change it).

                  - It commutes because if you change one of them, the other must change. This is beneficial as any change to the collection name, or role name, will necessarily preserve the mapping of morphisms (i.e. because if lets say a role is created via the morphism`Create: absent -> present`, then the   lets say change the collection name from 'Meta' to lets say 'Base', that abstract, and now easily in practice the moprhism in the file systems or configuration or whatever, the structure of the 

                    - If the first part one of a FQCN is not present, then it is not a FQCN

                  - The rationale behind making the cURI OA.COLLECTIONS.ROLES/Meta/meta_role rather than OA.COLLECTIONS/Meta/Roles/meta_role is because the nothing commutes better than basic composition.

                    - Consider changing the collection name

                    - 1. a) OA.COLLECTIONS.ROLES, b) Meta, c) meta_roles

                    - 2. a) OA.COLLECTIONS, b) Meta, c) Roles, d) meta_role

                    - When undergoing a morphism, if i have to change 'meta' -> 'base', then the composition involves more steps with (2) than (1). Because its really annoying trying to create natural transformations, when you can just go BEFORE + CHANGE + AFTER, then BEFORE + CHANGE_1 + MIDDLE + CHANGE_2 + AFTER.

                  - And also Not every collection has roles necessarily. But everything meta_role generates is a role, so it makes common sense too.

            - specification

              - Adds 

          - 

          - Base Muxer{PENDING}

        - meta_playbook

        - meta_orchestrator

        - Shared BASE Role

          - Currently 

      - Playbooks

    - 'Network' Collection

      - Roles

        - network.openvswitch

          - Description: Manages the Installation and Configuration of the OpenVSwitch Software

        - network.network

          - Description: Entry point / Interface for interacting with the network collection. Delegates configurations containing the network subsystems to the other roles

          - Parameters: ...

          - 

        - network.ovs_bridge

  - OA Tools Catelogs

    - TerminalAI

    - nvim-orchestration-architect

    - 

# Steps

  - 1. Create OA.META.Playbooks

    - Need to move :OA_DEV/playbooks/...

    - 

  - Migrate Collections

    - OA_COLLECTIONS_PATH

      - [DEV] $HOME/repos/orchestration-architect/develop/collections

      - [LOCAL INSTALL] $HOME/.local/share/orchestration-architect/collections

      - [SYSTEM PACKAGE] /usr/share/orchestration-architect/collections

    - Deploy

