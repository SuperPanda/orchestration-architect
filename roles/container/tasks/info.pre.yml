# file: roles/container/tasks/info.pre.yml
# ========================================================
# AUTO-GENERATED FILE - DO NOT EDIT!
# This file is generated by an automated system.
# Any manual changes may be overwritten.
# ========================================================
# role: container
# version: 0.8.1
---


# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content



# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
- name: Display variables within the scope of the container role
  when: ansible_verbosity | int > 1
  ansible.builtin.debug:
    msg: |
      container: {{ container | default('not set') }}
      container_state: {{ container_state | default('not set') }}
      container_machine_name: {{ container_machine_name | default('not set') }}
      container_target: {{ container_target | default('not set') }}
      container_qemu: {{ container_qemu | default('not set') }}
      container_nspawn: {{ container_nspawn | default('not set') }}
      
- name: Display the variable used to set the container role parameters
  when: ansible_verbosity | int > 0 and container is defined
  ansible.builtin.debug:
    msg: |
      state: "{{ ((container | default({})).state is defined and(container | default({})).state!=omit) | ternary('container.state', (container_state is defined and container_state==container_params.state) | ternary('container_state', '(unset) defaults to: omit' ))  }}"
      machine_name: "{{ ((container | default({})).machine_name is defined and(container | default({})).machine_name!=omit) | ternary('container.machine_name', (container_machine_name is defined and container_machine_name==container_params.machine_name) | ternary('container_machine_name', '(unset) defaults to: omit' ))  }}"
      target: "{{ ((container | default({})).target is defined and(container | default({})).target!=omit) | ternary('container.target', (container_target is defined and container_target==container_params.target) | ternary('container_target', '(unset) defaults to: omit' ))  }}"
      qemu: "{{ ((container | default({})).qemu is defined and(container | default({})).qemu!=omit) | ternary('container.qemu', (container_qemu is defined and container_qemu==container_params.qemu) | ternary('container_qemu', '(unset) defaults to: omit' ))  }}"
      nspawn: "{{ ((container | default({})).nspawn is defined and(container | default({})).nspawn!=omit) | ternary('container.nspawn', (container_nspawn is defined and container_nspawn==container_params.nspawn) | ternary('container_nspawn', '(unset) defaults to: omit' ))  }}"
      

# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content

