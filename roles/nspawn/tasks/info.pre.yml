# file: roles/nspawn/tasks/info.pre.yml
# ========================================================
# AUTO-GENERATED FILE - DO NOT EDIT!
# This file is generated by an automated system.
# Any manual changes may be overwritten.
# ========================================================
# role: nspawn
# version: 0.8.1
---


# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content



# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
- name: Display variables within the scope of the nspawn role
  when: ansible_verbosity | int > 1
  ansible.builtin.debug:
    msg: |
      nspawn: {{ nspawn | default('not set') }}
      nspawn_state: {{ nspawn_state | default('not set') }}
      nspawn_machine_name: {{ nspawn_machine_name | default('not set') }}
      nspawn_target: {{ nspawn_target | default('not set') }}
      nspawn_command_template: {{ nspawn_command_template | default('not set') }}
      nspawn_overlays: {{ nspawn_overlays | default('not set') }}
      nspawn_extras: {{ nspawn_extras | default('not set') }}
      
- name: Display the variable used to set the nspawn role parameters
  when: ansible_verbosity | int > 0 and nspawn is defined
  ansible.builtin.debug:
    msg: |
      state: "{{ ((nspawn | default({})).state is defined and(nspawn | default({})).state!=omit) | ternary('nspawn.state', (nspawn_state is defined and nspawn_state==nspawn_params.state) | ternary('nspawn_state', '(unset) defaults to: omit' ))  }}"
      machine_name: "{{ ((nspawn | default({})).machine_name is defined and(nspawn | default({})).machine_name!=omit) | ternary('nspawn.machine_name', (nspawn_machine_name is defined and nspawn_machine_name==nspawn_params.machine_name) | ternary('nspawn_machine_name', '(unset) defaults to: omit' ))  }}"
      target: "{{ ((nspawn | default({})).target is defined and(nspawn | default({})).target!=omit) | ternary('nspawn.target', (nspawn_target is defined and nspawn_target==nspawn_params.target) | ternary('nspawn_target', '(unset) defaults to: omit' ))  }}"
      command_template: "{{ ((nspawn | default({})).command_template is defined and(nspawn | default({})).command_template!=omit) | ternary('nspawn.command_template', (nspawn_command_template is defined and nspawn_command_template==nspawn_params.command_template) | ternary('nspawn_command_template', '(unset) defaults to: omit' ))  }}"
      overlays: "{{ ((nspawn | default({})).overlays is defined and(nspawn | default({})).overlays!=omit) | ternary('nspawn.overlays', (nspawn_overlays is defined and nspawn_overlays==nspawn_params.overlays) | ternary('nspawn_overlays', '(unset) defaults to: omit' ))  }}"
      extras: "{{ ((nspawn | default({})).extras is defined and(nspawn | default({})).extras!=omit) | ternary('nspawn.extras', (nspawn_extras is defined and nspawn_extras==nspawn_params.extras) | ternary('nspawn_extras', '(unset) defaults to: omit' ))  }}"
      

# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content

