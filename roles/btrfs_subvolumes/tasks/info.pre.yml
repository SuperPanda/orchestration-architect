# file: roles/btrfs_subvolumes/tasks/info.pre.yml
# ========================================================
# AUTO-GENERATED FILE - DO NOT EDIT!
# This file is generated by an automated system.
# Any manual changes may be overwritten.
# ========================================================
# role: btrfs_subvolumes
# version: 0.8.1
---


# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content



# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
- name: Display variables within the scope of the btrfs_subvolumes role
  when: ansible_verbosity | int > 1
  ansible.builtin.debug:
    msg: |
      btrfs_subvolumes: {{ btrfs_subvolumes | default('not set') }}
      btrfs_subvolumes_state: {{ btrfs_subvolumes_state | default('not set') }}
      btrfs_subvolumes_subvols: {{ btrfs_subvolumes_subvols | default('not set') }}
      btrfs_subvolumes_fs_label: {{ btrfs_subvolumes_fs_label | default('not set') }}
      btrfs_subvolumes_fs_uuid: {{ btrfs_subvolumes_fs_uuid | default('not set') }}
      
- name: Display the variable used to set the btrfs_subvolumes role parameters
  when: ansible_verbosity | int > 0 and btrfs_subvolumes is defined
  ansible.builtin.debug:
    msg: |
      state: "{{ ((btrfs_subvolumes | default({})).state is defined and(btrfs_subvolumes | default({})).state!=omit) | ternary('btrfs_subvolumes.state', (btrfs_subvolumes_state is defined and btrfs_subvolumes_state==btrfs_subvolumes_params.state) | ternary('btrfs_subvolumes_state', '(unset) defaults to: omit' ))  }}"
      subvols: "{{ ((btrfs_subvolumes | default({})).subvols is defined and(btrfs_subvolumes | default({})).subvols!=omit) | ternary('btrfs_subvolumes.subvols', (btrfs_subvolumes_subvols is defined and btrfs_subvolumes_subvols==btrfs_subvolumes_params.subvols) | ternary('btrfs_subvolumes_subvols', '(unset) defaults to: omit' ))  }}"
      fs_label: "{{ ((btrfs_subvolumes | default({})).fs_label is defined and(btrfs_subvolumes | default({})).fs_label!=omit) | ternary('btrfs_subvolumes.fs_label', (btrfs_subvolumes_fs_label is defined and btrfs_subvolumes_fs_label==btrfs_subvolumes_params.fs_label) | ternary('btrfs_subvolumes_fs_label', '(unset) defaults to: omit' ))  }}"
      fs_uuid: "{{ ((btrfs_subvolumes | default({})).fs_uuid is defined and(btrfs_subvolumes | default({})).fs_uuid!=omit) | ternary('btrfs_subvolumes.fs_uuid', (btrfs_subvolumes_fs_uuid is defined and btrfs_subvolumes_fs_uuid==btrfs_subvolumes_params.fs_uuid) | ternary('btrfs_subvolumes_fs_uuid', '(unset) defaults to: omit' ))  }}"
      

# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content

