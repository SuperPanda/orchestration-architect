# file: roles/mount/tasks/info.pre.yml
# ========================================================
# AUTO-GENERATED FILE - DO NOT EDIT!
# This file is generated by an automated system.
# Any manual changes may be overwritten.
# ========================================================
# role: mount
# version: 0.8.1
---


# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content



# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
- name: Display variables within the scope of the mount role
  when: ansible_verbosity | int > 1
  ansible.builtin.debug:
    msg: |
      mount: {{ mount | default('not set') }}
      mount_state: {{ mount_state | default('not set') }}
      mount_map: {{ mount_map | default('not set') }}
      mount_target: {{ mount_target | default('not set') }}
      mount_source: {{ mount_source | default('not set') }}
      
- name: Display the variable used to set the mount role parameters
  when: ansible_verbosity | int > 0 and mount is defined
  ansible.builtin.debug:
    msg: |
      state: "{{ ((mount | default({})).state is defined and(mount | default({})).state!=omit) | ternary('mount.state', (mount_state is defined and mount_state==mount_params.state) | ternary('mount_state', '(unset) defaults to: omit' ))  }}"
      map: "{{ ((mount | default({})).map is defined and(mount | default({})).map!=omit) | ternary('mount.map', (mount_map is defined and mount_map==mount_params.map) | ternary('mount_map', '(unset) defaults to: omit' ))  }}"
      target: "{{ ((mount | default({})).target is defined and(mount | default({})).target!=omit) | ternary('mount.target', (mount_target is defined and mount_target==mount_params.target) | ternary('mount_target', '(unset) defaults to: omit' ))  }}"
      source: "{{ ((mount | default({})).source is defined and(mount | default({})).source!=omit) | ternary('mount.source', (mount_source is defined and mount_source==mount_params.source) | ternary('mount_source', '(unset) defaults to: omit' ))  }}"
      

# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content

