# file: roles/qemu_runtime/tasks/info.pre.yml
# ========================================================
# AUTO-GENERATED FILE - DO NOT EDIT!
# This file is generated by an automated system.
# Any manual changes may be overwritten.
# ========================================================
# role: qemu_runtime
# version: 0.8.1
---


# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content



# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
- name: Display variables within the scope of the qemu_runtime role
  when: ansible_verbosity | int > 1
  ansible.builtin.debug:
    msg: |
      qemu_runtime: {{ qemu_runtime | default('not set') }}
      qemu_runtime_state: {{ qemu_runtime_state | default('not set') }}
      qemu_runtime_machine_name: {{ qemu_runtime_machine_name | default('not set') }}
      qemu_runtime_device_path: {{ qemu_runtime_device_path | default('not set') }}
      qemu_runtime_memory: {{ qemu_runtime_memory | default('not set') }}
      qemu_runtime_cpu_cores: {{ qemu_runtime_cpu_cores | default('not set') }}
      qemu_runtime_sockets: {{ qemu_runtime_sockets | default('not set') }}
      qemu_runtime_smp: {{ qemu_runtime_smp | default('not set') }}
      qemu_runtime_vga: {{ qemu_runtime_vga | default('not set') }}
      qemu_runtime_spice_socket: {{ qemu_runtime_spice_socket | default('not set') }}
      qemu_runtime_port_forwarding: {{ qemu_runtime_port_forwarding | default('not set') }}
      qemu_runtime_extras: {{ qemu_runtime_extras | default('not set') }}
      
- name: Display the variable used to set the qemu_runtime role parameters
  when: ansible_verbosity | int > 0 and qemu_runtime is defined
  ansible.builtin.debug:
    msg: |
      state: "{{ ((qemu_runtime | default({})).state is defined and(qemu_runtime | default({})).state!=omit) | ternary('qemu_runtime.state', (qemu_runtime_state is defined and qemu_runtime_state==qemu_runtime_params.state) | ternary('qemu_runtime_state', '(unset) defaults to: omit' ))  }}"
      machine_name: "{{ ((qemu_runtime | default({})).machine_name is defined and(qemu_runtime | default({})).machine_name!=omit) | ternary('qemu_runtime.machine_name', (qemu_runtime_machine_name is defined and qemu_runtime_machine_name==qemu_runtime_params.machine_name) | ternary('qemu_runtime_machine_name', '(unset) defaults to: omit' ))  }}"
      device_path: "{{ ((qemu_runtime | default({})).device_path is defined and(qemu_runtime | default({})).device_path!=omit) | ternary('qemu_runtime.device_path', (qemu_runtime_device_path is defined and qemu_runtime_device_path==qemu_runtime_params.device_path) | ternary('qemu_runtime_device_path', '(unset) defaults to: omit' ))  }}"
      memory: "{{ ((qemu_runtime | default({})).memory is defined and(qemu_runtime | default({})).memory!=omit) | ternary('qemu_runtime.memory', (qemu_runtime_memory is defined and qemu_runtime_memory==qemu_runtime_params.memory) | ternary('qemu_runtime_memory', '(unset) defaults to: omit' ))  }}"
      cpu_cores: "{{ ((qemu_runtime | default({})).cpu_cores is defined and(qemu_runtime | default({})).cpu_cores!=omit) | ternary('qemu_runtime.cpu_cores', (qemu_runtime_cpu_cores is defined and qemu_runtime_cpu_cores==qemu_runtime_params.cpu_cores) | ternary('qemu_runtime_cpu_cores', '(unset) defaults to: omit' ))  }}"
      sockets: "{{ ((qemu_runtime | default({})).sockets is defined and(qemu_runtime | default({})).sockets!=omit) | ternary('qemu_runtime.sockets', (qemu_runtime_sockets is defined and qemu_runtime_sockets==qemu_runtime_params.sockets) | ternary('qemu_runtime_sockets', '(unset) defaults to: omit' ))  }}"
      smp: "{{ ((qemu_runtime | default({})).smp is defined and(qemu_runtime | default({})).smp!=omit) | ternary('qemu_runtime.smp', (qemu_runtime_smp is defined and qemu_runtime_smp==qemu_runtime_params.smp) | ternary('qemu_runtime_smp', '(unset) defaults to: omit' ))  }}"
      vga: "{{ ((qemu_runtime | default({})).vga is defined and(qemu_runtime | default({})).vga!=omit) | ternary('qemu_runtime.vga', (qemu_runtime_vga is defined and qemu_runtime_vga==qemu_runtime_params.vga) | ternary('qemu_runtime_vga', '(unset) defaults to: omit' ))  }}"
      spice_socket: "{{ ((qemu_runtime | default({})).spice_socket is defined and(qemu_runtime | default({})).spice_socket!=omit) | ternary('qemu_runtime.spice_socket', (qemu_runtime_spice_socket is defined and qemu_runtime_spice_socket==qemu_runtime_params.spice_socket) | ternary('qemu_runtime_spice_socket', '(unset) defaults to: omit' ))  }}"
      port_forwarding: "{{ ((qemu_runtime | default({})).port_forwarding is defined and(qemu_runtime | default({})).port_forwarding!=omit) | ternary('qemu_runtime.port_forwarding', (qemu_runtime_port_forwarding is defined and qemu_runtime_port_forwarding==qemu_runtime_params.port_forwarding) | ternary('qemu_runtime_port_forwarding', '(unset) defaults to: omit' ))  }}"
      extras: "{{ ((qemu_runtime | default({})).extras is defined and(qemu_runtime | default({})).extras!=omit) | ternary('qemu_runtime.extras', (qemu_runtime_extras is defined and qemu_runtime_extras==qemu_runtime_params.extras) | ternary('qemu_runtime_extras', '(unset) defaults to: omit' ))  }}"
      

# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content

