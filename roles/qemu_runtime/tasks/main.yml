# file: roles/qemu_runtime/tasks/main.yml
# ========================================================
# AUTO-GENERATED FILE - DO NOT EDIT!
# This file is generated by an automated system.
# Any manual changes may be overwritten.
# ========================================================
# role: qemu_runtime
# version: 0.8.1
---


# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content


# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message

- name: Display supplied arguments to qemu_runtime when verbosity is set
  when: (ansible_verbosity | int)  > 1
  ansible.builtin.debug:
    msg: |
      qemu_runtime: {{ qemu_runtime | default('not set') }}
      qemu_runtime_state: {{ qemu_runtime_state | default('not set') }}
      qemu_runtime_machine_name: {{ qemu_runtime_machine_name | default('not set') }}
      qemu_runtime_device_path: {{ qemu_runtime_device_path | default('not set') }}
      qemu_runtime_memory: {{ qemu_runtime_memory | default('not set') }}
      qemu_runtime_cpu_cores: {{ qemu_runtime_cpu_cores | default('not set') }}
      qemu_runtime_sockets: {{ qemu_runtime_sockets | default('not set') }}
      qemu_runtime_smp: {{ qemu_runtime_smp | default('not set') }}
      qemu_runtime_vga: {{ qemu_runtime_vga | default('not set') }}
      qemu_runtime_spice_socket: {{ qemu_runtime_spice_socket | default('not set') }}
      qemu_runtime_port_forwarding: {{ qemu_runtime_port_forwarding | default('not set') }}
      qemu_runtime_extras: {{ qemu_runtime_extras | default('not set') }}
      
- debug:
    msg: |
      qemu_runtime_params:
        state: "{{ (qemu_runtime | default({})).state is defined | ternary(qemu_runtime.state, qemu_runtime_state | default(omit, true)) }}"
        machine_name: "{{ (qemu_runtime | default({})).machine_name is defined | ternary(qemu_runtime.machine_name, qemu_runtime_machine_name | default(omit, true)) }}"
        device_path: "{{ (qemu_runtime | default({})).device_path is defined | ternary(qemu_runtime.device_path, qemu_runtime_device_path | default(omit, true)) }}"
        memory: "{{ (qemu_runtime | default({})).memory is defined | ternary(qemu_runtime.memory, qemu_runtime_memory | default(omit, true)) }}"
        cpu_cores: "{{ (qemu_runtime | default({})).cpu_cores is defined | ternary(qemu_runtime.cpu_cores, qemu_runtime_cpu_cores | default(omit, true)) }}"
        sockets: "{{ (qemu_runtime | default({})).sockets is defined | ternary(qemu_runtime.sockets, qemu_runtime_sockets | default(omit, true)) }}"
        smp: "{{ (qemu_runtime | default({})).smp is defined | ternary(qemu_runtime.smp, qemu_runtime_smp | default(omit, true)) }}"
        vga: "{{ (qemu_runtime | default({})).vga is defined | ternary(qemu_runtime.vga, qemu_runtime_vga | default(omit, true)) }}"
        spice_socket: "{{ (qemu_runtime | default({})).spice_socket is defined | ternary(qemu_runtime.spice_socket, qemu_runtime_spice_socket | default(omit, true)) }}"
        port_forwarding: "{{ (qemu_runtime | default({})).port_forwarding is defined | ternary(qemu_runtime.port_forwarding, qemu_runtime_port_forwarding | default(omit, true)) }}"
        extras: "{{ (qemu_runtime | default({})).extras is defined | ternary(qemu_runtime.extras, qemu_runtime_extras | default(omit, true)) }}"
        
- name: Pass configuration parameters to the qemu_runtime role
  vars:
    qemu_runtime_params:
      state: "{{ (qemu_runtime | default({})).state is defined | ternary(qemu_runtime.state, qemu_runtime_state | default(omit, true)) }}"
      machine_name: "{{ (qemu_runtime | default({})).machine_name is defined | ternary(qemu_runtime.machine_name, qemu_runtime_machine_name | default(omit, true)) }}"
      device_path: "{{ (qemu_runtime | default({})).device_path is defined | ternary(qemu_runtime.device_path, qemu_runtime_device_path | default(omit, true)) }}"
      memory: "{{ (qemu_runtime | default({})).memory is defined | ternary(qemu_runtime.memory, qemu_runtime_memory | default(omit, true)) }}"
      cpu_cores: "{{ (qemu_runtime | default({})).cpu_cores is defined | ternary(qemu_runtime.cpu_cores, qemu_runtime_cpu_cores | default(omit, true)) }}"
      sockets: "{{ (qemu_runtime | default({})).sockets is defined | ternary(qemu_runtime.sockets, qemu_runtime_sockets | default(omit, true)) }}"
      smp: "{{ (qemu_runtime | default({})).smp is defined | ternary(qemu_runtime.smp, qemu_runtime_smp | default(omit, true)) }}"
      vga: "{{ (qemu_runtime | default({})).vga is defined | ternary(qemu_runtime.vga, qemu_runtime_vga | default(omit, true)) }}"
      spice_socket: "{{ (qemu_runtime | default({})).spice_socket is defined | ternary(qemu_runtime.spice_socket, qemu_runtime_spice_socket | default(omit, true)) }}"
      port_forwarding: "{{ (qemu_runtime | default({})).port_forwarding is defined | ternary(qemu_runtime.port_forwarding, qemu_runtime_port_forwarding | default(omit, true)) }}"
      extras: "{{ (qemu_runtime | default({})).extras is defined | ternary(qemu_runtime.extras, qemu_runtime_extras | default(omit, true)) }}"
      
  ansible.builtin.include_tasks:
    file: "{{ (qemu_runtime_params.state==omit) | ternary('info', qemu_runtime_params.state) }}.yml"


# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content
