# file: roles/qemu/tasks/info.pre.yml
# ========================================================
# AUTO-GENERATED FILE - DO NOT EDIT!
# This file is generated by an automated system.
# Any manual changes may be overwritten.
# ========================================================
# role: qemu
# version: 0.8.1
---


# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content



# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
- name: Display variables within the scope of the qemu role
  when: ansible_verbosity | int > 1
  ansible.builtin.debug:
    msg: |
      qemu: {{ qemu | default('not set') }}
      qemu_state: {{ qemu_state | default('not set') }}
      qemu_machine_name: {{ qemu_machine_name | default('not set') }}
      qemu_target: {{ qemu_target | default('not set') }}
      qemu_command_template: {{ qemu_command_template | default('not set') }}
      
- name: Display the variable used to set the qemu role parameters
  when: ansible_verbosity | int > 0 and qemu is defined
  ansible.builtin.debug:
    msg: |
      state: "{{ ((qemu | default({})).state is defined and(qemu | default({})).state!=omit) | ternary('qemu.state', (qemu_state is defined and qemu_state==qemu_params.state) | ternary('qemu_state', '(unset) defaults to: omit' ))  }}"
      machine_name: "{{ ((qemu | default({})).machine_name is defined and(qemu | default({})).machine_name!=omit) | ternary('qemu.machine_name', (qemu_machine_name is defined and qemu_machine_name==qemu_params.machine_name) | ternary('qemu_machine_name', '(unset) defaults to: omit' ))  }}"
      target: "{{ ((qemu | default({})).target is defined and(qemu | default({})).target!=omit) | ternary('qemu.target', (qemu_target is defined and qemu_target==qemu_params.target) | ternary('qemu_target', '(unset) defaults to: omit' ))  }}"
      command_template: "{{ ((qemu | default({})).command_template is defined and(qemu | default({})).command_template!=omit) | ternary('qemu.command_template', (qemu_command_template is defined and qemu_command_template==qemu_params.command_template) | ternary('qemu_command_template', '(unset) defaults to: omit' ))  }}"
      

# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content

