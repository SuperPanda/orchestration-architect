# file: roles/nspawn_runtime/tasks/info.pre.yml
# ========================================================
# AUTO-GENERATED FILE - DO NOT EDIT!
# This file is generated by an automated system.
# Any manual changes may be overwritten.
# ========================================================
# role: nspawn_runtime
# version: 0.8.1
---


# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content



# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
- name: Display variables within the scope of the nspawn_runtime role
  when: ansible_verbosity | int > 1
  ansible.builtin.debug:
    msg: |
      nspawn_runtime: {{ nspawn_runtime | default('not set') }}
      nspawn_runtime_state: {{ nspawn_runtime_state | default('not set') }}
      nspawn_runtime_target: {{ nspawn_runtime_target | default('not set') }}
      nspawn_runtime_name: {{ nspawn_runtime_name | default('not set') }}
      nspawn_runtime_overlays: {{ nspawn_runtime_overlays | default('not set') }}
      nspawn_runtime_extras: {{ nspawn_runtime_extras | default('not set') }}
      
- name: Display the variable used to set the nspawn_runtime role parameters
  when: ansible_verbosity | int > 0 and nspawn_runtime is defined
  ansible.builtin.debug:
    msg: |
      state: "{{ ((nspawn_runtime | default({})).state is defined and(nspawn_runtime | default({})).state!=omit) | ternary('nspawn_runtime.state', (nspawn_runtime_state is defined and nspawn_runtime_state==nspawn_runtime_params.state) | ternary('nspawn_runtime_state', '(unset) defaults to: omit' ))  }}"
      target: "{{ ((nspawn_runtime | default({})).target is defined and(nspawn_runtime | default({})).target!=omit) | ternary('nspawn_runtime.target', (nspawn_runtime_target is defined and nspawn_runtime_target==nspawn_runtime_params.target) | ternary('nspawn_runtime_target', '(unset) defaults to: omit' ))  }}"
      name: "{{ ((nspawn_runtime | default({})).name is defined and(nspawn_runtime | default({})).name!=omit) | ternary('nspawn_runtime.name', (nspawn_runtime_name is defined and nspawn_runtime_name==nspawn_runtime_params.name) | ternary('nspawn_runtime_name', '(unset) defaults to: omit' ))  }}"
      overlays: "{{ ((nspawn_runtime | default({})).overlays is defined and(nspawn_runtime | default({})).overlays!=omit) | ternary('nspawn_runtime.overlays', (nspawn_runtime_overlays is defined and nspawn_runtime_overlays==nspawn_runtime_params.overlays) | ternary('nspawn_runtime_overlays', '(unset) defaults to: omit' ))  }}"
      extras: "{{ ((nspawn_runtime | default({})).extras is defined and(nspawn_runtime | default({})).extras!=omit) | ternary('nspawn_runtime.extras', (nspawn_runtime_extras is defined and nspawn_runtime_extras==nspawn_runtime_params.extras) | ternary('nspawn_runtime_extras', '(unset) defaults to: omit' ))  }}"
      

# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content

