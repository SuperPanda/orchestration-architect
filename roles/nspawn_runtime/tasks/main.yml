# file: roles/nspawn_runtime/tasks/main.yml
# ========================================================
# AUTO-GENERATED FILE - DO NOT EDIT!
# This file is generated by an automated system.
# Any manual changes may be overwritten.
# ========================================================
# role: nspawn_runtime
# version: 0.8.1
---


# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content


# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message

- name: Display supplied arguments to nspawn_runtime when verbosity is set
  when: (ansible_verbosity | int)  > 1
  ansible.builtin.debug:
    msg: |
      nspawn_runtime: {{ nspawn_runtime | default('not set') }}
      nspawn_runtime_state: {{ nspawn_runtime_state | default('not set') }}
      nspawn_runtime_target: {{ nspawn_runtime_target | default('not set') }}
      nspawn_runtime_name: {{ nspawn_runtime_name | default('not set') }}
      nspawn_runtime_overlays: {{ nspawn_runtime_overlays | default('not set') }}
      nspawn_runtime_extras: {{ nspawn_runtime_extras | default('not set') }}
      
- debug:
    msg: |
      nspawn_runtime_params:
        state: "{{ (nspawn_runtime | default({})).state is defined | ternary(nspawn_runtime.state, nspawn_runtime_state | default(omit, true)) }}"
        target: "{{ (nspawn_runtime | default({})).target is defined | ternary(nspawn_runtime.target, nspawn_runtime_target | default(omit, true)) }}"
        name: "{{ (nspawn_runtime | default({})).name is defined | ternary(nspawn_runtime.name, nspawn_runtime_name | default(omit, true)) }}"
        overlays: "{{ (nspawn_runtime | default({})).overlays is defined | ternary(nspawn_runtime.overlays, nspawn_runtime_overlays | default(omit, true)) }}"
        extras: "{{ (nspawn_runtime | default({})).extras is defined | ternary(nspawn_runtime.extras, nspawn_runtime_extras | default(omit, true)) }}"
        
- name: Pass configuration parameters to the nspawn_runtime role
  vars:
    nspawn_runtime_params:
      state: "{{ (nspawn_runtime | default({})).state is defined | ternary(nspawn_runtime.state, nspawn_runtime_state | default(omit, true)) }}"
      target: "{{ (nspawn_runtime | default({})).target is defined | ternary(nspawn_runtime.target, nspawn_runtime_target | default(omit, true)) }}"
      name: "{{ (nspawn_runtime | default({})).name is defined | ternary(nspawn_runtime.name, nspawn_runtime_name | default(omit, true)) }}"
      overlays: "{{ (nspawn_runtime | default({})).overlays is defined | ternary(nspawn_runtime.overlays, nspawn_runtime_overlays | default(omit, true)) }}"
      extras: "{{ (nspawn_runtime | default({})).extras is defined | ternary(nspawn_runtime.extras, nspawn_runtime_extras | default(omit, true)) }}"
      
  ansible.builtin.include_tasks:
    file: "{{ (nspawn_runtime_params.state==omit) | ternary('info', nspawn_runtime_params.state) }}.yml"


# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content
