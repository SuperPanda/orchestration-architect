# file: roles/filesystem/tasks/info.pre.yml
# ========================================================
# AUTO-GENERATED FILE - DO NOT EDIT!
# This file is generated by an automated system.
# Any manual changes may be overwritten.
# ========================================================
# role: filesystem
# version: 0.8.1
---


# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content



# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
- name: Display variables within the scope of the filesystem role
  when: ansible_verbosity | int > 1
  ansible.builtin.debug:
    msg: |
      filesystem: {{ filesystem | default('not set') }}
      filesystem_state: {{ filesystem_state | default('not set') }}
      filesystem_label: {{ filesystem_label | default('not set') }}
      filesystem_opts: {{ filesystem_opts | default('not set') }}
      filesystem_type: {{ filesystem_type | default('not set') }}
      filesystem_path: {{ filesystem_path | default('not set') }}
      
- name: Display the variable used to set the filesystem role parameters
  when: ansible_verbosity | int > 0 and filesystem is defined
  ansible.builtin.debug:
    msg: |
      state: "{{ ((filesystem | default({})).state is defined and(filesystem | default({})).state!=omit) | ternary('filesystem.state', (filesystem_state is defined and filesystem_state==filesystem_params.state) | ternary('filesystem_state', '(unset) defaults to: omit' ))  }}"
      label: "{{ ((filesystem | default({})).label is defined and(filesystem | default({})).label!=omit) | ternary('filesystem.label', (filesystem_label is defined and filesystem_label==filesystem_params.label) | ternary('filesystem_label', '(unset) defaults to: omit' ))  }}"
      opts: "{{ ((filesystem | default({})).opts is defined and(filesystem | default({})).opts!=omit) | ternary('filesystem.opts', (filesystem_opts is defined and filesystem_opts==filesystem_params.opts) | ternary('filesystem_opts', '(unset) defaults to: omit' ))  }}"
      type: "{{ ((filesystem | default({})).type is defined and(filesystem | default({})).type!=omit) | ternary('filesystem.type', (filesystem_type is defined and filesystem_type==filesystem_params.type) | ternary('filesystem_type', '(unset) defaults to: omit' ))  }}"
      path: "{{ ((filesystem | default({})).path is defined and(filesystem | default({})).path!=omit) | ternary('filesystem.path', (filesystem_path is defined and filesystem_path==filesystem_params.path) | ternary('filesystem_path', '(unset) defaults to: omit' ))  }}"
      

# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content

