# file: roles/meta_orchestrator/tasks/info.pre.yml
# ========================================================
# AUTO-GENERATED FILE - DO NOT EDIT!
# This file is generated by an automated system.
# Any manual changes may be overwritten.
# ========================================================
# role: meta_orchestrator
# version: 0.8.1
---


# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content



# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
- name: Display variables within the scope of the meta_orchestrator role
  when: ansible_verbosity | int > 1
  ansible.builtin.debug:
    msg: |
      meta_orchestrator: {{ meta_orchestrator | default('not set') }}
      meta_orchestrator_state: {{ meta_orchestrator_state | default('not set') }}
      meta_orchestrator_base_path: {{ meta_orchestrator_base_path | default('not set') }}
      meta_orchestrator_check_mode: {{ meta_orchestrator_check_mode | default('not set') }}
      meta_orchestrator_specifications_path: {{ meta_orchestrator_specifications_path | default('not set') }}
      meta_orchestrator_specifications_version: {{ meta_orchestrator_specifications_version | default('not set') }}
      meta_orchestrator_specifications_map: {{ meta_orchestrator_specifications_map | default('not set') }}
      meta_orchestrator_specifications: {{ meta_orchestrator_specifications | default('not set') }}
      meta_orchestrator_schema: {{ meta_orchestrator_schema | default('not set') }}
      meta_orchestrator_operations: {{ meta_orchestrator_operations | default('not set') }}
      meta_orchestrator_scope: {{ meta_orchestrator_scope | default('not set') }}
      
- name: Display the variable used to set the meta_orchestrator role parameters
  when: ansible_verbosity | int > 0 and meta_orchestrator is defined
  ansible.builtin.debug:
    msg: |
      state: "{{ ((meta_orchestrator | default({})).state is defined and(meta_orchestrator | default({})).state!=omit) | ternary('meta_orchestrator.state', (meta_orchestrator_state is defined and meta_orchestrator_state==meta_orchestrator_params.state) | ternary('meta_orchestrator_state', '(unset) defaults to: omit' ))  }}"
      base_path: "{{ ((meta_orchestrator | default({})).base_path is defined and(meta_orchestrator | default({})).base_path!=omit) | ternary('meta_orchestrator.base_path', (meta_orchestrator_base_path is defined and meta_orchestrator_base_path==meta_orchestrator_params.base_path) | ternary('meta_orchestrator_base_path', '(unset) defaults to: omit' ))  }}"
      check_mode: "{{ ((meta_orchestrator | default({})).check_mode is defined and(meta_orchestrator | default({})).check_mode!=omit) | ternary('meta_orchestrator.check_mode', (meta_orchestrator_check_mode is defined and meta_orchestrator_check_mode==meta_orchestrator_params.check_mode) | ternary('meta_orchestrator_check_mode', '(unset) defaults to: omit' ))  }}"
      specifications_path: "{{ ((meta_orchestrator | default({})).specifications_path is defined and(meta_orchestrator | default({})).specifications_path!=omit) | ternary('meta_orchestrator.specifications_path', (meta_orchestrator_specifications_path is defined and meta_orchestrator_specifications_path==meta_orchestrator_params.specifications_path) | ternary('meta_orchestrator_specifications_path', '(unset) defaults to: omit' ))  }}"
      specifications_version: "{{ ((meta_orchestrator | default({})).specifications_version is defined and(meta_orchestrator | default({})).specifications_version!=omit) | ternary('meta_orchestrator.specifications_version', (meta_orchestrator_specifications_version is defined and meta_orchestrator_specifications_version==meta_orchestrator_params.specifications_version) | ternary('meta_orchestrator_specifications_version', '(unset) defaults to: omit' ))  }}"
      specifications_map: "{{ ((meta_orchestrator | default({})).specifications_map is defined and(meta_orchestrator | default({})).specifications_map!=omit) | ternary('meta_orchestrator.specifications_map', (meta_orchestrator_specifications_map is defined and meta_orchestrator_specifications_map==meta_orchestrator_params.specifications_map) | ternary('meta_orchestrator_specifications_map', '(unset) defaults to: omit' ))  }}"
      specifications: "{{ ((meta_orchestrator | default({})).specifications is defined and(meta_orchestrator | default({})).specifications!=omit) | ternary('meta_orchestrator.specifications', (meta_orchestrator_specifications is defined and meta_orchestrator_specifications==meta_orchestrator_params.specifications) | ternary('meta_orchestrator_specifications', '(unset) defaults to: omit' ))  }}"
      schema: "{{ ((meta_orchestrator | default({})).schema is defined and(meta_orchestrator | default({})).schema!=omit) | ternary('meta_orchestrator.schema', (meta_orchestrator_schema is defined and meta_orchestrator_schema==meta_orchestrator_params.schema) | ternary('meta_orchestrator_schema', '(unset) defaults to: omit' ))  }}"
      operations: "{{ ((meta_orchestrator | default({})).operations is defined and(meta_orchestrator | default({})).operations!=omit) | ternary('meta_orchestrator.operations', (meta_orchestrator_operations is defined and meta_orchestrator_operations==meta_orchestrator_params.operations) | ternary('meta_orchestrator_operations', '(unset) defaults to: omit' ))  }}"
      scope: "{{ ((meta_orchestrator | default({})).scope is defined and(meta_orchestrator | default({})).scope!=omit) | ternary('meta_orchestrator.scope', (meta_orchestrator_scope is defined and meta_orchestrator_scope==meta_orchestrator_params.scope) | ternary('meta_orchestrator_scope', '(unset) defaults to: omit' ))  }}"
      

# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content

