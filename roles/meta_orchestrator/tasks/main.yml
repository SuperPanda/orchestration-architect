# ========================================================
# AUTO-GENERATED FILE - DO NOT EDIT!
# This file is generated by an automated system.
# Any manual changes may be overwritten.
# ========================================================
# role: meta_orchestrator
# file: roles/meta_orchestrator/tasks/main.yml
# version: 0.8.1
---


# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 30_managed_block_before_content



# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 50_content_region_message

- name: Display supplied arguments to meta_orchestrator when verbosity is set
  when: (ansible_verbosity | int)  > 1
  ansible.builtin.debug:
    msg: |
      meta_orchestrator: {{ meta_orchestrator | default('not set') }}
      meta_orchestrator_state: {{ meta_orchestrator_state | default('not set') }}
      meta_orchestrator_base_path: {{ meta_orchestrator_base_path | default('not set') }}
      meta_orchestrator_check_mode: {{ meta_orchestrator_check_mode | default('not set') }}
      meta_orchestrator_specifications_path: {{ meta_orchestrator_specifications_path | default('not set') }}
      meta_orchestrator_specifications_version: {{ meta_orchestrator_specifications_version | default('not set') }}
      meta_orchestrator_specifications_map: {{ meta_orchestrator_specifications_map | default('not set') }}
      meta_orchestrator_specifications: {{ meta_orchestrator_specifications | default('not set') }}
      meta_orchestrator_schema: {{ meta_orchestrator_schema | default('not set') }}
      meta_orchestrator_operations: {{ meta_orchestrator_operations | default('not set') }}
      meta_orchestrator_scope: {{ meta_orchestrator_scope | default('not set') }}
      
- debug:
    msg: |
      meta_orchestrator_params:
        state: "{{ (meta_orchestrator | default({})).state is defined | ternary(meta_orchestrator.state, meta_orchestrator_state | default(omit, true)) }}"
        base_path: "{{ (meta_orchestrator | default({})).base_path is defined | ternary(meta_orchestrator.base_path, meta_orchestrator_base_path | default(omit, true)) }}"
        check_mode: "{{ (meta_orchestrator | default({})).check_mode is defined | ternary(meta_orchestrator.check_mode, meta_orchestrator_check_mode | default(omit, true)) }}"
        specifications_path: "{{ (meta_orchestrator | default({})).specifications_path is defined | ternary(meta_orchestrator.specifications_path, meta_orchestrator_specifications_path | default(omit, true)) }}"
        specifications_version: "{{ (meta_orchestrator | default({})).specifications_version is defined | ternary(meta_orchestrator.specifications_version, meta_orchestrator_specifications_version | default(omit, true)) }}"
        specifications_map: "{{ (meta_orchestrator | default({})).specifications_map is defined | ternary(meta_orchestrator.specifications_map, meta_orchestrator_specifications_map | default(omit, true)) }}"
        specifications: "{{ (meta_orchestrator | default({})).specifications is defined | ternary(meta_orchestrator.specifications, meta_orchestrator_specifications | default(omit, true)) }}"
        schema: "{{ (meta_orchestrator | default({})).schema is defined | ternary(meta_orchestrator.schema, meta_orchestrator_schema | default(omit, true)) }}"
        operations: "{{ (meta_orchestrator | default({})).operations is defined | ternary(meta_orchestrator.operations, meta_orchestrator_operations | default(omit, true)) }}"
        scope: "{{ (meta_orchestrator | default({})).scope is defined | ternary(meta_orchestrator.scope, meta_orchestrator_scope | default(omit, true)) }}"
        
- name: Pass configuration parameters to the meta_orchestrator role
  vars:
    meta_orchestrator_params:
      state: "{{ (meta_orchestrator | default({})).state is defined | ternary(meta_orchestrator.state, meta_orchestrator_state | default(omit, true)) }}"
      base_path: "{{ (meta_orchestrator | default({})).base_path is defined | ternary(meta_orchestrator.base_path, meta_orchestrator_base_path | default(omit, true)) }}"
      check_mode: "{{ (meta_orchestrator | default({})).check_mode is defined | ternary(meta_orchestrator.check_mode, meta_orchestrator_check_mode | default(omit, true)) }}"
      specifications_path: "{{ (meta_orchestrator | default({})).specifications_path is defined | ternary(meta_orchestrator.specifications_path, meta_orchestrator_specifications_path | default(omit, true)) }}"
      specifications_version: "{{ (meta_orchestrator | default({})).specifications_version is defined | ternary(meta_orchestrator.specifications_version, meta_orchestrator_specifications_version | default(omit, true)) }}"
      specifications_map: "{{ (meta_orchestrator | default({})).specifications_map is defined | ternary(meta_orchestrator.specifications_map, meta_orchestrator_specifications_map | default(omit, true)) }}"
      specifications: "{{ (meta_orchestrator | default({})).specifications is defined | ternary(meta_orchestrator.specifications, meta_orchestrator_specifications | default(omit, true)) }}"
      schema: "{{ (meta_orchestrator | default({})).schema is defined | ternary(meta_orchestrator.schema, meta_orchestrator_schema | default(omit, true)) }}"
      operations: "{{ (meta_orchestrator | default({})).operations is defined | ternary(meta_orchestrator.operations, meta_orchestrator_operations | default(omit, true)) }}"
      scope: "{{ (meta_orchestrator | default({})).scope is defined | ternary(meta_orchestrator.scope, meta_orchestrator_scope | default(omit, true)) }}"
      
  ansible.builtin.include_tasks:
    file: "{{ (meta_orchestrator_params.state==omit) | ternary('info', meta_orchestrator_params.state) }}.yml"


# BEGIN ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content
# END ORCHESTRATION ARCHITECT MANAGED BLOCK 90_managed_block_after_content
